import { ExecutionAPI } from "./api";
import { GetStatusResponse } from "./types";
export interface Page {
    number: number;
    values: Record<string, unknown>[];
}
export declare class Paginator {
    private client;
    private executionId;
    private pageSize;
    private currentPageNumber;
    pageCache: Map<number, Page>;
    totalRows: number;
    private constructor();
    static new(client: ExecutionAPI, executionStatus: GetStatusResponse, pageLimit: number): Promise<Paginator>;
    maxPage(): number;
    nextPage(): Promise<Page | undefined>;
    previousPage(): Promise<Page | undefined>;
    lastPage(): Promise<Page | undefined>;
    getPage(n: number): Promise<Page | undefined>;
    getCurrentPageValues(): Page;
}
