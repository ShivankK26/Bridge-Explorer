export declare const logPrefix = "dune-client:";
/**
 * utility sleep method.
 * @param seconds number of seconds to sleep for.
 * @returns void
 */
export declare function sleep(seconds: number): Promise<void>;
/**
 * Computes the difference between a given timestamp and now (in hours)
 * @param timestamp
 * @returns time difference between input `timestamp` and now (in hours)
 */
export declare function ageInHours(timestamp: Date | string): number;
/**
 *
 * @param obj Used to populate partial payloads with required defaults
 * @param defaults
 * @returns
 */
export declare function withDefaults<T>(obj: T, defaults: Partial<T>): T;
